/**
 * main.js
 * Entry point and initialization for Solace Music Player
 * Bootstraps the player and starts the application
 */

import { MusicPlayer } from './player.js';

// Initialize player
const player = new MusicPlayer();
